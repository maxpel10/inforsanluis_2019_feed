import 'package:flutter/material.dart';

import 'MyApp.dart';

class Reposteos extends StatelessWidget {
  List<Posteo> reposteados;

  Reposteos(this.reposteados);

  Widget build(BuildContext context) {
    return Container(
        //TODO: mostrar los posteos del feed que han sido reposteados.
        );
  }
}
